#include <iostream>

using namespace std;

//not complete
int main() {
    const int numbers = 10;
    int groupNumbers[numbers];
    int presentedNumber;

    cout << "Enter 10 numbers: \n";
    for (int i = 0; i < 10; i++)
    {
        cin >> groupNumbers[i];
    }
    
    cout << "Enter a number: ";
    cin >> presentedNumber;

    if (presentedNumber == NNumbers(groupNumbers[numbers]))
    {
        cout;
    }

    // make function for reading the numbers of the index
    
}

void NNumbers(int groupnum[i]) {

    for (int a = 0; a < 10; a++)
        {
            
        }

}
